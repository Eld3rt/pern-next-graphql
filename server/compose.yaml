services:
  database:
    image: postgres
    ports:
      - 5011:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    env_file:
      - .env
  cache:
    image: redis
    ports:
      - 5012:6379
volumes:
  pgdata:
